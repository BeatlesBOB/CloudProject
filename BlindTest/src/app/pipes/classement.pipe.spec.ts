import { ClassementPipe } from './classement.pipe';

describe('ClassementPipe', () => {
  it('create an instance', () => {
    const pipe = new ClassementPipe();
    expect(pipe).toBeTruthy();
  });
});
